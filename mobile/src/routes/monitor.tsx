import { TabScreen } from "../components/TabScreen";

export default function MonitorRoute() {
  return (
    <TabScreen
      title="Monitor"
      hint="CPU / RAM / session count land in T18.18 via the T18.5 bridge."
    />
  );
}
